name: 'de_kata'

config-version: 2
version: '0.1'

profile: 'de_kata'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"
    - "logs"

require-dbt-version: [">=1.0.0", "<2.0.0"]

#seeds:
#  +docs:
#    node_color: '#cd7f32'

models:
  de_kata:
    materialized: table
    staging:
      +materialized: view